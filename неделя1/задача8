#include <iostream>
#include <algorithm>
#include <math.h>
 
using namespace std;
 
int main() {
    int a, b, n;
    cin >> a >> b >> n;
    int k = ((a * 100) + b) * n;
    cout << k / 100 << " " << k % 100 << endl; 
    return 0;
}
