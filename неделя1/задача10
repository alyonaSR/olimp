#include <iostream>
#include <cmath>

using namespace std;

int main() {
    int h, a, b;
    cin >> h >> a >> b;

    int days = ceil((h - a) / (double)(a - b)) + 1;
    
    cout << days << endl;

    return 0;
}
