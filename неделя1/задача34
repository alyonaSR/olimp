#include <iostream>
#include <vector>

using namespace std;

int main() {
    int n, a;
    cin >> n;
    int ans = 0;
    for(int i = 0; i < n; i++){
        cin >> a;
        if (a > 10){
            ans += a - 10;
        }
    }
    cout << ans << endl; 
}
