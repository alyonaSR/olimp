#include <iostream>
#include <cmath>

using namespace std;

int main() {
    int x, x1, y, y1;
    cin >> x >> y >> x1 >> y1;
    if(x == x1 || y == y1)
        cout << "YES" << endl;
    else
        cout << "NO" << endl; 
    return 0;
}
