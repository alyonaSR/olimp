#include <iostream>
 
using namespace std;
 
int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);
    long long a1, a2, a3; 
    cin>>a1>>a2>>a3; 
    if(a1==a2+a3 or a2==a1+a3 or a3==a1+a2) cout<<"YES"; 
    else cout<<"NO"; 
 
    return 0;
}
